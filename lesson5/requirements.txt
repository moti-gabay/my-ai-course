# Install the CPU-only torch wheel FIRST (see README_SETUP.md):
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# "torch==2.13.0" below is then satisfied by 2.13.0+cpu and pip won't pull the CUDA build.
torch==2.13.0

# Agents
langgraph==1.2.11
langchain==1.3.15
langchain-core==1.6.0
langchain-anthropic==1.7.0
anthropic==0.121.0
pydantic==2.13.3
python-dotenv==1.2.2

# Retrieval (Assignment 3 index + reranker)
faiss-cpu==1.15.0
sentence-transformers==6.0.0
transformers==5.15.0
langchain-huggingface==1.2.2
langchain-community==0.4.2
langchain-text-splitters==1.1.2
pypdf==6.14.2
cryptography==47.0.0  # pypdf needs it for the AES-encrypted Allstate PDF

# Evaluation + reporting
numpy==2.2.6
pandas==2.2.3
openpyxl==3.1.5

# Dashboard
streamlit==1.64.0
plotly==7.1.0
